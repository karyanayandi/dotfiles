<* include "lazydocker-base" *>
  # No live config reload. ANSI colors follow the terminal palette.
  theme:
    activeBorderColor: [green, bold]
    inactiveBorderColor: [white]
    optionsTextColor: [blue]
    selectedLineBgColor: [default, reverse]
